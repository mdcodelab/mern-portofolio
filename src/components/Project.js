// Project.js

import React from "react";

const Project = ({ project }) => {
  return (
    <div className="">
      <h3>{project.title}</h3>
    </div>
  );
};

export default Project;
